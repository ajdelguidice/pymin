from setuptools import find_packages, setup

setup(
    name='sollib',
    packages=find_packages(include=['sollib']),
    version='0.0.1',
    description='A library for manipulating Flash Shared Object (.sol) files',
    author='ajdelguidice',
    license='MIT',
    install_requires=[''],
    setup_requires=[''],
    tests_require=[''],
    test_suite='',
)